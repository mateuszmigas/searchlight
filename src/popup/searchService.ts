const parseQuery = (query: string) => {};

type SearchItem = {
  id: string;
  title: string;
  url: string;
};

type SearchResult = {
  favourites: any[];
  bookmakrs: any[];
  tabs: any[];
  history: any[];
};
